# Python dependencies for MuseumSpark data management scripts

# JSON Schema validation
jsonschema>=4.20.0

# Data validation for MRD guardrails
pydantic>=2.6.0

# PDF text extraction (used by build-museum-list-csv-from-narm.py)
pdfplumber>=0.11.0

# HTML parsing (used by build-walker-reciprocal-csv.py)
beautifulsoup4>=4.12.3

# HTML parsing for conservative official-site scraping (used by enrich-open-data.py)
# (BeautifulSoup uses Python's built-in html.parser by default; no extra parser required.)
