# Python dependencies for MuseumSpark data management scripts

# JSON Schema validation
jsonschema>=4.20.0

# Data validation for MRD guardrails
pydantic>=2.6.0

# PDF text extraction (used by build-museum-list-csv-from-narm.py)
pdfplumber>=0.11.0

# HTML parsing (used by build-walker-reciprocal-csv.py)
beautifulsoup4>=4.12.3

# HTML parsing for conservative official-site scraping (used by enrich-open-data.py)
# (BeautifulSoup uses Python's built-in html.parser by default; no extra parser required.)

# HTML to Markdown conversion for clean LLM input
html2text>=2020.1.16

# Address extraction and parsing
pyap>=0.3.1  # Python Address Parser - optimized for US addresses
usaddress>=0.5.10  # ML-based US address parser
extruct>=0.13.0  # Structured data extraction (JSON-LD, microdata, RDFa)

# Geocoding APIs (optional - graceful degradation if not available)
googlemaps>=4.10.0  # Google Places API for address lookup
geopy>=2.3.0  # OpenStreetMap Nominatim geocoding

# Business location APIs (optional - for enriched museum data)
yelpapi>=2.5.0  # Yelp Fusion API for business hours, photos, reviews

# LLM providers (optional for AI enrichment)
openai>=1.0.0
anthropic>=0.20.0
